<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">
    <!-- Define default log directory if not specified by system property -->
    <property name="LOG_DIR" value="${LOG_DIR:-./logs}" />

    <!-- Register custom converter for colored fields (console only) -->
    <conversionRule conversionWord="coloredMsg"
                    converterClass="io.canopy.engine.logging.logback.ColoredFieldsConverter" />

    <!-- ============================================== -->
    <!-- Console Appender (Terminal Output with Colors) -->
    <!-- ============================================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Use custom converter %coloredMsg for colored fields -->
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%cyan(%logger{36})] - %coloredMsg%n</pattern>
        </encoder>
    </appender>

    <!-- ============================================== -->
    <!-- Console Appender for WARN/ERROR only          -->
    <!-- ============================================== -->
    <appender name="CONSOLE_ERRORS" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%cyan(%logger{36})] - %coloredMsg%n</pattern>
        </encoder>
    </appender>

    <!-- ============================================== -->
    <!-- File Appender (Plain Text Log File - NO COLORS)-->
    <!-- ============================================== -->
    <appender name="CLIENT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/client.log</file>
        <immediateFlush>true</immediateFlush>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/client.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <!-- Use standard %msg - no colors -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ENGINE_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/engine.log</file>
        <immediateFlush>true</immediateFlush>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/engine.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <!-- Use standard %msg - no colors -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/canopy.log</file>
        <immediateFlush>true</immediateFlush>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/canopy.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <!-- Use standard %msg - no colors -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ============================================== -->
    <!-- JSON File Appender (Structured Logs - NO COLORS) jsonl-->
    <!-- ============================================== -->
    <appender name="CLIENT_FILE_JSONL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/client.jsonl</file>
        <immediateFlush>true</immediateFlush>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/client.%d{yyyy-MM-dd}.%i.jsonl</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>mdc</includeMdcKeyName>
            <includeContext>true</includeContext>
            <includeCallerData>false</includeCallerData>
        </encoder>
    </appender>

    <appender name="ENGINE_FILE_JSONL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/engine.jsonl</file>
        <immediateFlush>true</immediateFlush>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/engine.%d{yyyy-MM-dd}.%i.jsonl</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>mdc</includeMdcKeyName>
            <includeContext>true</includeContext>
            <includeCallerData>false</includeCallerData>
        </encoder>
    </appender>

    <appender name="FILE_JSONL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/canopy.jsonl</file>
        <immediateFlush>true</immediateFlush>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/canopy.%d{yyyy-MM-dd}.%i.jsonl</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>mdc</includeMdcKeyName>
            <includeContext>true</includeContext>
            <includeCallerData>false</includeCallerData>
        </encoder>
    </appender>

    <!-- ============================================== -->
    <!-- Sifting Appender (Separate log file per class)-->
    <!-- ============================================== -->
<!--    <appender name="SIFT" class="ch.qos.logback.classic.sift.SiftingAppender">-->
<!--        &lt;!&ndash; Use custom discriminator to extract simple class name &ndash;&gt;-->
<!--        <discriminator class="io.canopy.engine.logging.logback.ClassNameDiscriminator">-->
<!--            <key>className</key>-->
<!--            <defaultValue>unknown</defaultValue>-->
<!--        </discriminator>-->

<!--        <sift>-->
<!--            <appender name="FILE-${className}" class="ch.qos.logback.core.rolling.RollingFileAppender">-->
<!--                <file>${LOG_DIR}/clsses/${className}.log</file>-->
<!--                <immediateFlush>true</immediateFlush>-->

<!--                <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">-->
<!--                    <fileNamePattern>${LOG_DIR}/classes/${className}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>-->
<!--                    <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">-->
<!--                        <maxFileSize>10MB</maxFileSize>-->
<!--                    </timeBasedFileNamingAndTriggeringPolicy>-->
<!--                    <maxHistory>30</maxHistory>-->
<!--                </rollingPolicy>-->

<!--                <encoder>-->
<!--                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level - %msg%n</pattern>-->
<!--                </encoder>-->
<!--            </appender>-->
<!--        </sift>-->
<!--    </appender>-->

    <!-- ============================================== -->
    <!-- Sifting Appender JSON (Separate JSON file per class)-->
    <!-- ============================================== -->
<!--    <appender name="SIFT_JSONL" class="ch.qos.logback.classic.sift.SiftingAppender">-->
<!--        &lt;!&ndash; Use custom discriminator to extract simple class name &ndash;&gt;-->
<!--        <discriminator class="io.canopy.engine.logging.logback.ClassNameDiscriminator">-->
<!--            <key>className</key>-->
<!--            <defaultValue>unknown</defaultValue>-->
<!--        </discriminator>-->

<!--        <sift>-->
<!--            <appender name="JSON-${className}" class="ch.qos.logback.core.rolling.RollingFileAppender">-->
<!--                <file>${LOG_DIR}/classes/${className}.jsonl</file>-->
<!--                <immediateFlush>true</immediateFlush>-->

<!--                <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">-->
<!--                    <fileNamePattern>${LOG_DIR}/classes/${className}.%d{yyyy-MM-dd}.%i.jsonl</fileNamePattern>-->
<!--                    <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">-->
<!--                        <maxFileSize>10MB</maxFileSize>-->
<!--                    </timeBasedFileNamingAndTriggeringPolicy>-->
<!--                    <maxHistory>30</maxHistory>-->
<!--                </rollingPolicy>-->

<!--                <encoder class="net.logstash.logback.encoder.LogstashEncoder">-->
<!--                    <includeMdcKeyName>mdc</includeMdcKeyName>-->
<!--                    <includeContext>true</includeContext>-->
<!--                    <includeCallerData>false</includeCallerData>-->
<!--                </encoder>-->
<!--            </appender>-->
<!--        </sift>-->
<!--    </appender>-->

    <!-- ============================================== -->
    <!-- Engine Logger (FILE ONLY - No Console)         -->
    <!-- Except WARN/ERROR which go to console too      -->
    <!-- Must come BEFORE generic io.canopy logger      -->
    <!-- ============================================== -->
    <logger name="io.canopy.engine" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE_ERRORS" />
        <appender-ref ref="ENGINE_FILE" />
        <appender-ref ref="ENGINE_FILE_JSONL" />
<!--        <appender-ref ref="SIFT" />-->
<!--        <appender-ref ref="SIFT_JSONL" />-->
    </logger>

    <logger name="io.canopy.client" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="CLIENT_FILE" />
        <appender-ref ref="CLIENT_FILE_JSONL" />
        <!--        <appender-ref ref="SIFT" />-->
        <!--        <appender-ref ref="SIFT_JSONL" />-->
    </logger>

    <!-- Other canopy logs (Console + Files) -->
    <logger name="io.canopy" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="FILE_JSONL" />
<!--        <appender-ref ref="SIFT" />-->
<!--        <appender-ref ref="SIFT_JSONL" />-->
    </logger>

    <!-- ============================================== -->
    <!-- Root Logger Configuration                      -->
    <!-- ============================================== -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="FILE_JSONL" />
<!--        <appender-ref ref="SIFT" />-->
<!--        <appender-ref ref="SIFT_JSONL" />-->
    </root>


    <!-- Reduce noise -->
    <logger name="org.slf4j" level="WARN" />
    <logger name="ch.qos.logback" level="WARN" />

</configuration>

